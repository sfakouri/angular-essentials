import { Hover3Directive } from './hover3.directive';

describe('Hover3Directive', () => {
  it('should create an instance', () => {
    const directive = new Hover3Directive();
    expect(directive).toBeTruthy();
  });
});
