import { Hover2Directive } from './hover2.directive';

describe('Hover2Directive', () => {
  it('should create an instance', () => {
    const directive = new Hover2Directive();
    expect(directive).toBeTruthy();
  });
});
